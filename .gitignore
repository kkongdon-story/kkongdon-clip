# Extension signing keys - NEVER commit
extension/.keys/
extension/.keys/*.pem

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Claude Code / OMC planning state (local only)
.claude/
.omc/
extension/lib/.omc/

# Dev notes
setup-dev-remote.md

# 환경변수·비밀키 파일 (절대 커밋 금지)
*.env
.env.*
secrets.json
config.local.js
config.local.json
